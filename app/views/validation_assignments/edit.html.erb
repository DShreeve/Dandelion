<h1><%= @project.name %> > <%= @table.name %> > <%= @field.name %> > Editing <%= @validation.name %></h1>

<%= render 'form' %>


<%= link_to 'Back', project_table_field_path(@project, @table, @field), class: "btn btn-default" %>
<%= link_to 'Show', 
  project_table_field_validation_assignment_path(@project, @table, @field,@validation_assignment),
    class: "btn btn-success" %>