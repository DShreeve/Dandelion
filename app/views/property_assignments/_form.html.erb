<%= form_for([@project,@table,@field,@property_assignment], html: {class:"form-horizontal"}) do |f| %>
  <% if @property_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_assignment.errors.count, "error") %> prohibited this property_assignment from being saved:</h2>

      <ul>
      <% @property_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :property_id , class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :property_id, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Submit" , class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
